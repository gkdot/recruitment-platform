export type Role = "applicant" | "admin" | "super_admin";

// Role constants
export const Roles = {
  Applicant: "applicant" as Role,
  Admin: "admin" as Role,
  SuperAdmin: "super_admin" as Role,
};
